@model IEnumerable<Capstone.Models.Itinerary>

@{
    ViewData["Title"] = "ItineraryListByUser";
}

<h2>ItineraryListByUser</h2>

<p>
    <a asp-controller="Home" asp-action="CreateItinerary"><div>Create Itinerary</div></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                
                <a asp-action="Itinerary" asp-route-id="@item.ID">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
        </tr>
}
    </tbody>
</table>
